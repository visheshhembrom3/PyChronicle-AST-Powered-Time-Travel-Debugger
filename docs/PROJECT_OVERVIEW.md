# PyChronicle Project Overview

PyChronicle is an AST-powered Python time-travel debugger.

The project captures execution history, tracks variable changes,
stores execution data in SQLite, and allows historical state replay.

Core technologies:
- Python
- AST
- sys.settrace()
- SQLite
- Textual
- Click
- JSON
